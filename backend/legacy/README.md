# Legacy

`app_streamlit.py` — original Streamlit prototype (before FastAPI migration).

- Kept for reference only.
- Not used in production. Entry point is `main.py` (FastAPI).
- Run legacy: `streamlit run legacy/app_streamlit.py`
